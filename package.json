{
  "name": "mockprep-server",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint && prettier --check .",
    "format": "prettier --write .",
    "test": "turbo run test"
  },
  "prettier": "@mockprep/config/prettier",
  "devDependencies": {
    "@mockprep/config": "workspace:*",
    "prettier": "^3.9.9",
    "turbo": "^2.11.4",
    "typescript": "~5.9.3"
  }
}
